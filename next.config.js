/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // images: {
  //   loader: "akamai",
  //   path: "",
  // },
  // basePath: "/portfolio",
  // assetPrefix: "/portfolio",
  // Comment Lines images To assetPrefix To Run It Locally (Icon Won't Be Visible, That's Okay!) 
};

module.exports = nextConfig;